var w = prompt("Give me the width.");
//ask user for a width and save as w
var h = prompt("Give me the height.");
//ask user for a height and save as h

function findArea(width,height) {
//create a function that takes two parameters
  var area = width*height;
//Multiply width and height parameters
  return value;
//value is returned
}

var wallOne = findArea(w,h);
//pass w and h to the findArea function
window.alert(wallOne);
//popup area to screen